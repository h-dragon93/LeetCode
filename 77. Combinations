import itertools

class Solution:
    def combine(self, n: int, k: int) -> List[List[int]]:
        li = [i for i in range(1, n+1)]
        comb = list(map(list, itertools.combinations(li, k)))
        
        return comb
