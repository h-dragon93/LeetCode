class Solution:
    def isPalindrome(self, x: int) -> bool:
        strX = str(x)

        for i in range((len(strX)+1)//2) :
            if strX[i] == strX[-i-1] :
                continue
            else :
                return False
        return True
